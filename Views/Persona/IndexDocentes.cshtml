@model List<IDS325L___ProyectoFinal___Índice_académico.Models.Persona>


<h1>Gestoniar docentes</h1>

<a class="btn btn-danger btn-sm w-25 mt-2 mb-2 mt-3 mb-4" asp-action="CreateDocentes" asp-controller="Persona" asp-route-Matricula="0">Crear Docentes</a>

<table class="table">
    <thead>
        <tr>
            <th>Matricula</th>
            <th>Área académica</th>
            <th>Nombre</th>
            <th>Apellido</th>
            <th>Correo electronico</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
        {
            <tr>
                <td>@item.Matricula</td>
                <td>@item.CodigoArea</td>
                <td>@item.Nombre</td>
                <td>@item.Apellido</td>
                <td>@item.CorreoElectronico</td>
                <td>
                    <a class="btn btn-danger btn-sm" asp-action="CreateDocentes" asp-controller="Persona" asp-route-Matricula="@item.Matricula">Editar</a>
                    <a class="btn btn-danger btn-sm" asp-action="Delete" asp-controller="Persona" asp-route-Matricula="@item.Matricula">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>